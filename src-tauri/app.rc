// Embed the application manifest.
// CREATEPROCESS_MANIFEST_RESOURCE_ID = 1, RT_MANIFEST = 24.
// This is the only resource here — no VERSIONINFO — so it won't
// conflict with the version resource that tauri-build generates.
1 24 "app.manifest"
